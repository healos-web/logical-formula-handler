#include <string>
#include <stdlib.h>
#include <vector>
using namespace std;

vector<char> extractVariables(string formula_1, string formula_2) {
    vector<char> vec;
    return vec;
}

vector<vector<int>> generateCases(int variablesNumber) {
    vector<vector<int>> vec;
    return vec;
}

vector<int> calculate(string formula, vector<vector<int>> cases) {
    vector<int> vec;
    return vec;
}

bool isFollowing(vector<int> formulaValues_1, vector<int> formulaValues_2) {
    return true;
};

